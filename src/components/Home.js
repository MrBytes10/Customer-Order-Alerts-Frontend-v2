// src/components/Home.js
import React from 'react';
import { Typography } from '@mui/material';

function Home() {
  return (
    <div>
      <Typography variant="h5">Welcome to the Order Alert System</Typography>
      <Typography variant="body1">Use the navigation above to add customers or orders.</Typography>
    </div>
  );
}

export default Home;
